
GENERAL INFORMATION
===================

The 3GPP TS 38.413 version 15.4.0 S1 Application Protocol (NGAP) APER decoder.
Invoking `make` will compile the ASN.1 specifications from the
asn1/ngap-*.asn1 file.

OBTAINING THE NGAP SPECIFICATION
================================

To obtain a different version of NGAP ASN.1 specification, you should go to
	http://www.3gpp.org/ftp/Specs/html-info/36413.htm
and download any version of NGAP specification you like.

A .ZIP file with a Microsoft Word .DOC files will download shortly.

You should extract the ASN.1 modules from the chapter 9 of that .DOC file.
Be careful not to copy any preambles, chapter titles and other non-ASN.1 text.

ngap-dump USAGE
===============

The ngap-dump utility may be used to dump the contents of a APER-encoded
NGAP protocol data unit. Since NGAP specification contains multiple PDUs,
a PDU must be selected manually using -p <PDU> command line option:

	./ngap-dump -iaper -p NGAP-PDU message.per

The list of recognized PDUs may be obtained using `-p list`.

The full list of recognized command line options may be obtained with

	> ./ngap-dump -h

